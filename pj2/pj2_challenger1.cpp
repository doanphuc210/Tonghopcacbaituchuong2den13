#include <iostream>
using namespace std;

int main() {
    int so1 = 50;
    int so2 = 100;
    int total = so1 + so2;

    cout << "Tổng của hai số " << so1 << " và " << so2 << " là: " << total << endl;

    return 0;
}
